# ตั้งค่า LINE Official Account ให้ระบบแจ้งซ่อมส่งข้อความเข้ากลุ่ม LINE

> **สถานะ: รอไว้ก่อน (when ready).** Nik ตัดสินใจ 2026-09-14 ให้ทำตัวเลขแดงบนเมนูแจ้งซ่อมในแอปก่อน
> ส่วน LINE ทำทีหลังเมื่อสะดวก **ส่วน A ทำได้ทุกเมื่อ** ไม่ต้องรออะไร
> **ส่วน B ต้องรอ** จนกว่าจะบอกว่าโค้ดฝั่งแอป (เส้นทางรับ webhook) ขึ้น production แล้ว —
> ตอนนี้ยังไม่มี ทำส่วน B ก่อนจะไม่มีอะไรตอบ

**ทำไมต้องเป็นวิธีนี้:** LINE Notify ปิดบริการไปแล้วตั้งแต่ 31 มี.ค. 2025 ทางเดียวที่เหลือคือ
LINE Official Account (OA) ที่เปิด Messaging API แล้วเชิญ OA เข้ากลุ่ม LINE ธรรมดา (ไม่ใช่ OpenChat)
เหมือนเชิญเพื่อนคนหนึ่ง แอปจะส่งข้อความเข้ากลุ่มผ่าน OA นั้น

**ไม่ต้องมีอีเมล/เบอร์ใหม่:** LINE Business ID หนึ่งบัญชี (ล็อกอินด้วย LINE ส่วนตัวของ Nik หรืออีเมลเดียว)
เปิด OA ได้ถึง 100 บัญชี OA แจ้งซ่อมจึงอยู่ใต้ล็อกอินเดียวกับ OA หน้าร้านที่มีอยู่แล้ว

**สิ่งที่ Nik ต้องได้จากส่วน A — สองค่า ใส่ใน Vercel:**

| ค่า | หน้าตา | ได้จาก |
|---|---|---|
| `LINE_CHANNEL_ACCESS_TOKEN` | ตัวอักษรยาวมาก (หลายร้อยตัว) | Developers Console → แท็บ Messaging API |
| `LINE_CHANNEL_SECRET` | ตัวอักษร 32 ตัว | Developers Console → แท็บ Basic settings |

**Group ID ไม่ต้องหาเอง** — แอปจะจับเองตอนพิมพ์คำว่า `เชื่อมต่อ` ในกลุ่ม (ส่วน B)

**ห้ามส่งสองค่านี้ทางแชท ทางไลน์ หรือวางลงในข้อความถึงใคร** — วางลงใน Vercel เท่านั้น
ถ้าเผลอส่งไปแล้ว ให้ไปกด **Reissue** ในหน้าเดียวกับที่ออกค่านั้น ค่าเก่าจะใช้ไม่ได้ทันที

---

## ส่วน A — ทำได้ทุกเมื่อ (ประมาณ 20 นาที)

### A1. สร้าง OA แจ้งซ่อม ใต้ Business ID เดิม

1. เปิด https://manager.line.biz → เข้าสู่ระบบด้วยบัญชีเดียวกับที่ดูแล OA หน้าร้าน
2. ในหน้ารายชื่อบัญชี กด **สร้างบัญชี LINE Official Account** (Create) — จะได้ OA ใหม่อีกหนึ่งบัญชีในรายชื่อเดิม
3. ตั้งชื่อ เช่น `แจ้งซ่อม สมพงศ์` — ชื่อนี้คือชื่อที่โผล่ในกลุ่มตอนส่งข้อความ
4. เลือกประเภทธุรกิจตามจริง กรอกที่เหลือตามที่ระบบถาม → ยืนยัน
5. เข้าหน้าบัญชีที่เพิ่งสร้าง จด **Basic ID** (`@xxxxxxx`) ไว้ใช้ตอน A5

### A2. เปิดใช้ Messaging API

1. ในหน้าบัญชี OA แจ้งซ่อม กด **ตั้งค่า** (Settings, รูปเฟืองมุมขวาบน) → เมนูซ้าย **Messaging API**
2. กด **เปิดใช้งาน Messaging API** (Enable)
3. เลือก/สร้าง **Provider** — ถ้า OA หน้าร้านมี Provider อยู่แล้วเลือกอันเดิมได้ ไม่มีก็สร้างชื่อ `สมพงศ์ ซีฟู้ด`
4. Privacy policy / Terms เว้นว่างได้ → ตกลง

### A3. เอาสองค่า

1. เปิด https://developers.line.biz/console → Provider → channel ชื่อเดียวกับ OA แจ้งซ่อม
2. แท็บ **Basic settings** → เลื่อนหา **Channel secret** → คัดลอก = `LINE_CHANNEL_SECRET`
3. แท็บ **Messaging API** → ล่างสุด **Channel access token (long-lived)** → **Issue** → คัดลอก = `LINE_CHANNEL_ACCESS_TOKEN`
4. ไปวางทั้งสองค่าใน Vercel ตาม A4 **ทันที** อย่าเก็บพักในโน้ต/ไลน์/อีเมล

### A4. ใส่ค่าลง Vercel

1. https://vercel.com → โปรเจกต์ **sompong-system** (โปรเจกต์นี้เท่านั้น ไม่ใช่ app-five-orpin-49)
2. **Settings** → **Environment Variables** → **Add** ทีละตัว:
   - Key `LINE_CHANNEL_ACCESS_TOKEN` → Value จาก A3.3
   - Key `LINE_CHANNEL_SECRET` → Value จาก A3.2
   - Environment: ติ๊ก **Production** (ติ๊ก Preview ด้วยได้)
3. **Save** — ค่ามีผลกับการ deploy ครั้งถัดไป ไม่ต้องกดอะไรเพิ่ม

### A5. ตั้งค่าไม่ให้ OA พูดเองมั่ว และอนุญาตให้เข้ากลุ่ม

ใน OA Manager → **ตั้งค่า**

1. **การตอบกลับ** (Response settings):
   - **ข้อความตอบกลับอัตโนมัติ** (Auto-response) → **ปิด** — ไม่ปิด OA จะตอบทุกข้อความในกลุ่ม
   - **ข้อความทักทายเพื่อนใหม่** (Greeting) → **ปิด**
   - **Webhook** → ยังไม่ต้องยุ่ง (ส่วน B)
2. **ตั้งค่าบัญชี** (Account settings) → หัวข้อ **ฟีเจอร์** (Features)
   → **เข้าร่วมกลุ่มแชทและแชทหลายคน** (Allow joining group chats) → **อนุญาต**
   - หาไม่เจอ: Developers Console แท็บ Messaging API มีบรรทัด **Allow bot to join group chats** → **Edit** พาไปหน้าเดียวกัน
   - ข้ามข้อนี้ การเชิญใน A6 จะล้มเหลวเงียบ ๆ

### A6. เชิญ OA เข้ากลุ่มแจ้งซ่อม

กลุ่มต้องเป็น **กลุ่ม LINE ธรรมดา** ไม่ใช่ OpenChat และมีคนเท่าที่จำเป็น (ดูโควตาท้ายเอกสาร)

1. ในแอป LINE → เพิ่มเพื่อน → ค้นหาด้วย ID → พิมพ์ Basic ID จาก A1 → เพิ่ม (เฉพาะคนที่จะเชิญต้องเป็นเพื่อนกับ OA)
2. เปิดกลุ่ม → เมนู (≡) → **เชิญ** → เลือก OA → เชิญ
3. OA โผล่เป็นสมาชิกทันที ถ้าไม่ กลับไปดู A5.2

**จบส่วน A** — บอกว่า "ส่วน A เสร็จแล้ว" พอ ไม่ต้องบอกค่า

---

## ส่วน B — รอจนกว่าจะบอกว่าเส้นทาง webhook ขึ้นแล้ว (3 นาที)

แอปจะมีเส้นทางรับข้อความจาก LINE ที่ `https://sompong-system.vercel.app/api/line/webhook`
เมื่อมีใครพิมพ์ `เชื่อมต่อ` ในกลุ่มที่ OA อยู่ แอปจะจำ Group ID ของกลุ่มนั้นไว้เอง แล้วตอบกลับว่า `เชื่อมต่อแล้ว`
ไม่ต้องคัดลอก ID ไม่ต้องใช้เว็บของคนอื่น

### B1. ชี้ webhook มาที่แอป

1. Developers Console → channel → แท็บ **Messaging API** → **Webhook settings**
2. **Webhook URL** → Edit → วาง `https://sompong-system.vercel.app/api/line/webhook` → **Update**
3. กด **Verify** → ต้องขึ้น **Success** (ถ้าไม่ แปลว่าโค้ดฝั่งแอปยังไม่ขึ้น — หยุดแล้วแจ้ง)
4. เปิดสวิตช์ **Use webhook**

### B2. เชื่อมกลุ่ม

1. ในกลุ่มแจ้งซ่อม พิมพ์ `เชื่อมต่อ` (คำนี้คำเดียว)
2. OA ตอบ `เชื่อมต่อแล้ว` ภายในไม่กี่วินาที → **เสร็จ** แอปจำกลุ่มนี้แล้ว
3. พิมพ์ในกลุ่มอื่นที่ OA อยู่ด้วย จะย้ายไปกลุ่มนั้นแทน (จำได้ครั้งละกลุ่มเดียว) — จึงเชิญ OA ไว้กลุ่มเดียวพอ

ถ้าไม่มีอะไรตอบ ไล่ตามลำดับ: **Use webhook** เปิดอยู่ไหม → OA อยู่ในกลุ่มไหม → A5.2 อนุญาตแล้วไหม
→ Verify ใน B1.3 ขึ้น Success ไหม

**จบส่วน B** — จากนั้นทดสอบด้วยการแจ้งซ่อมจริง 1 รายการในแอป ข้อความควรเข้ากลุ่มภายในไม่กี่วินาที

---

## โควตาข้อความ — ทำไมกลุ่มต้องเล็ก

LINE นับข้อความที่แอป "ส่งเข้าไป" (push) **ต่อผู้รับ** ไม่ใช่ต่อครั้ง: push เข้ากลุ่มที่มี 5 คน = ใช้โควตา 5 ข้อความ
(ข้อความที่ OA "ตอบกลับ" เช่น `เชื่อมต่อแล้ว` ไม่นับ)

แอปจะส่ง 2 ครั้งต่อรายการแจ้งซ่อม — ตอนแจ้ง และตอนซ่อมเสร็จ:

- กลุ่ม 3 คน → รายการละ 6 ข้อความ
- กลุ่ม 5 คน → รายการละ 10 ข้อความ

โควตาของแผนฟรีดูตัวเลขจริงที่ OA Manager → **แผนการใช้งาน** (Plan) — ตัวเลขเปลี่ยนได้ตามปีและประเทศ
จึงไม่เขียนไว้ตรงนี้ เอาตัวเลขนั้นหาร 10 = จำนวนรายการแจ้งซ่อมต่อเดือนที่กลุ่ม 5 คนรองรับได้
เกินโควตา LINE จะไม่ส่งส่วนที่เกิน — รายการแจ้งซ่อมยังถูกบันทึกในแอปตามปกติ แค่ไม่มีแจ้งเตือน

**คนที่ควรอยู่ในกลุ่ม:** คนที่รับผิดชอบซ่อม + หัวหน้าที่ต้องรู้ เท่านั้น คนแจ้งไม่จำเป็นต้องอยู่ — เขาเห็นสถานะในแอป

## เมื่อโค้ดฝั่งแอปพร้อม สิ่งที่มันจะทำ (บันทึกไว้เพื่อให้ตรงกัน)

- ส่งเข้ากลุ่มตอนแจ้ง: รูป, จุดที่เสีย, รายละเอียด, ชื่อคนแจ้ง, ลิงก์เปิดรายการ
- ส่งอีกครั้งตอนซ่อมเสร็จ ให้คนแจ้งรู้ทางไลน์โดยไม่ต้องเปิดแอป
- ส่งไม่สำเร็จ **ไม่ทำให้รายการหาย**: รายการถูกบันทึกเสมอ หน้าจอจะบอกว่า
  "บันทึกแล้ว แต่ส่งแจ้งเตือน LINE ไม่สำเร็จ"
